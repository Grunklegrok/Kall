# Security

Kall stores sensitive EEO, work authorization, clearance, contact, and reference fields encrypted. Production requires a non-default application secret, a dedicated sensitive-data encryption key, and a server database. Application submission and sensitive-field use require explicit user confirmation.
