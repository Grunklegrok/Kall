# Deployment

Use PostgreSQL, set APP_ENV=production, disable automatic table creation, configure strong application and encryption keys, run Alembic migrations, then start the FastAPI and Next.js services.
