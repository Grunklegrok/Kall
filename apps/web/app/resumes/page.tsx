'use client'; import {FormEvent,useState} from 'react'; const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000'; export default function Resumes(){const[m,setM]=useState('');async function upload(e:FormEvent<HTMLFormElement>){e.preventDefault();const t=localStorage.getItem('kall_token');const f=new FormData(e.currentTarget);const r=await fetch(`${API}/api/me/resumes`,{method:'POST',headers:{'Authorization':`Bearer ${t}`},body:f});const d=await r.json();setM(r.ok?`Uploaded ${d.name}`:(d.detail||'Upload failed'))}return <main className='shell'><header className='topbar'><div className='brand'>Kall Resume Studio</div><nav><a href='/dashboard'>Dashboard</a><a href='/profile'>Profile</a></nav></header><section className='grid'><article className='card'><h1>Upload a resume</h1><form className='form' onSubmit={upload}><input className='input' type='file' name='file' accept='.pdf,.docx' required/><button className='button'>Upload resume</button></form><p>{m}</p></article><article className='card'><h2>How Kall uses resumes</h2><p>Kall selects the closest base resume and prepares truthful role-specific emphasis for your review.</p></article></section></main>}