'use client'; import {FormEvent,useState} from 'react'; const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000'; export default function Login(){const[m,setM]=useState('');async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${API}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json();if(!r.ok)return setM(d.detail||'Login failed');localStorage.setItem('kall_token',d.access_token);location.href='/dashboard'}return <main className='shell'><div className='card' style={{maxWidth:520,margin:'70px auto'}}><div className='brand'>Kall</div><h1>Welcome back</h1><form className='form' onSubmit={go}><input className='input' name='email' type='email' placeholder='Email' required/><input className='input' name='password' type='password' placeholder='Password' required/><button className='button'>Log in</button></form><p>{m}</p></div></main>}